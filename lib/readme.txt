The 'lib' directory is for the external libraries (jar files).

To compile the project, you must add all the jar files in this 
directory to the Build Path (try right-clicking on files).

You may also use these jars in the homework.
