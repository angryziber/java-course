package net.azib.java.students.t010588.homework.importer;

import static org.junit.Assert.assertTrue;

import org.junit.Test;

/**
 * Tests if ConsoleImporter has athletes after user enters data via console
 *
 * @author Vjatseslav Rosin, 010588
 */
public class ConsoleImporterTest {

	/**
	 * Test method for
	 * {@link net.azib.java.students.t010588.homework.importer.ConsoleImporter#getAthletes()}.
	 *
	 * @throws WrongFormatException
	 */
	@Test
	public void testGetAthletes() throws WrongFormatException {
//		TODO: hangs the test: assertTrue(new ConsoleImporter().getAthletes().size() > 0);
	}
}
